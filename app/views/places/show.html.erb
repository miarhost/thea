
	<div class="newslettercontent">
		     <div class="leftside">
<%= image_tag(@place.image.url, version: :medium) if @place.image? %>
                <h1><%= @place.name %></h1>
			     <p><%= @place.description %></p>
		     </div>
             <div class="rightside">
             <p>Likes: </p>
             </div>
			
			
			</div>
									
					

